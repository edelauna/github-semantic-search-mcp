{
  "name": "workflow",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --ip=0.0.0.0 --experimental-vectorize-bind-to-prod",
    "start": "wrangler dev",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.24",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "~3.0.0",
    "wrangler": "^4.14.1"
  },
  "dependencies": {
    "gpt-tokenizer": "^2.9.0"
  }
}
